name: Build and push vLLM image

on:
  push:
    branches: [ main ]
    tags: [ "v*" ]

permissions:
  contents: read
  packages: write   # needed for GHCR
  id-token: write

env:
  IMAGE_REPO: ghcr.io/elizabetht/million-token-lab/vllm-serve

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Show initial disk usage
        run: |
          echo "Initial disk usage:"
          df -h

      - name: Free up disk space on runner
        run: |
          # Remove big preinstalled SDKs and tools we don't need
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /opt/hostedtoolcache || true

          # Clean any existing Docker stuff (if present)
          docker system prune -af --volumes || true

          echo "Disk usage after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image name
        id: vars
        run: |
          # lowercase owner
          OWNER=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${OWNER}/million-token-lab/vllm-serve"

          # If this is a tag like "v0.0.1", pull out "0.0.1"
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            # fallback for branch builds – you can choose what you want here
            VERSION="latest"
          fi

          echo "IMAGE=$IMAGE"   >> "$GITHUB_OUTPUT"
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.vars.outputs.IMAGE }}:${{ steps.vars.outputs.VERSION }}
            ${{ steps.vars.outputs.IMAGE }}:latest
      - name: Update RunPod pod to new image
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
          RUNPOD_POD_ID: ${{ secrets.RUNPOD_POD_ID }}
        run: |
          set -euo pipefail

          IMAGE_TAG="${{ steps.vars.outputs.IMAGE }}:${{ steps.vars.outputs.VERSION }}"

          echo "Updating RunPod pod ${RUNPOD_POD_ID} to image ${IMAGE_TAG}"

          # Minimal JSON body: just change the imageName
          cat <<EOF > body.json
          {
            "imageName": "${IMAGE_TAG}"
          }
          EOF

          echo "Request body:"
          cat body.json

          echo "Calling RunPod..."
          curl -i \
            --request POST \
            --url "https://rest.runpod.io/v1/pods/${RUNPOD_POD_ID}/update" \
            --header "Authorization: Bearer ${RUNPOD_API_KEY}" \
            --header "Content-Type: application/json" \
            --data @body.json
          
      

