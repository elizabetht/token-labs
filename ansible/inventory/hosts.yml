all:
  vars:
    ansible_user: nvidia
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    kubernetes_version: "1.32"
    pod_cidr: "10.244.0.0/16"

cluster:
  children:
    control_plane:
      hosts:
        spark-01:
          ansible_host: spark-01
    workers:
      hosts:
        spark-02:
          ansible_host: spark-02
