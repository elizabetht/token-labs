# LeaderWorkerSet — nvidia/GLM-4.7-NVFP4 with PP=2 across 2 DGX Sparks (agentic)
#
# Architecture:
#   spark-01 (leader): SGLang HTTP server, PP rank 0, layers 0–N/2
#   spark-02 (worker): SGLang PP rank 1, layers N/2–N
#   Communication: NCCL over ethernet (pod overlay, backed by 100 GbE)
#   Distributed init: torch.distributed via --dist-init-addr leader_ip:20000
#
# Model: nvidia/GLM-4.7-NVFP4
#   - 358B total params, selective NVFP4 (linear ops only; embeddings/norms stay BF16)
#   - Effective weight footprint: ~206 GB (170 GB FP4 + 36 GB BF16)
#   - PP=2 with mem-fraction-static 0.80: ~20 GB KV headroom — keep context short
#   - Tool-call and reasoning parsers enabled (glm47/glm45)
#
# Prerequisites:
#   1. Install LWS controller:
#      kubectl apply --server-side -f https://github.com/kubernetes-sigs/lws/releases/latest/download/manifests.yaml
#   2. Disable GPU time-slicing on both nodes:
#      kubectl label node spark-01 nvidia.com/gpu.sharing-strategy=none --overwrite
#      kubectl label node spark-02 nvidia.com/gpu.sharing-strategy=none --overwrite
#   3. Create PVCs:
#      kubectl apply -f deploy/glm-4.7/model-cache-pvcs.yaml
#   4. Create HF token secret if model requires auth:
#      kubectl create secret generic hf-token-secret --from-literal=hf-token=hf_xxx -n token-labs

apiVersion: leaderworkerset.x-k8s.io/v1
kind: LeaderWorkerSet
metadata:
  name: glm-4-7
  namespace: token-labs
  labels:
    app: glm-4-7
spec:
  replicas: 1
  leaderWorkerTemplate:
    size: 2                                    # 1 leader + 1 worker
    restartPolicy: RecreateGroupOnPodRestart
    leaderTemplate:
      metadata:
        labels:
          app: glm-4-7
          role: leader
          llm-d.ai/inference-serving: "true"
          llm-d.ai/model: glm-4-7
      spec:
        runtimeClassName: nvidia
        nodeSelector:
          kubernetes.io/hostname: spark-01
        tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        containers:
        - name: sglang
          image: lmsysorg/sglang:nightly-dev-cu13-20260318-cb1e63ab
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          ports:
          - containerPort: 8000
            name: http
          - containerPort: 20000
            name: dist-init
          resources:
            requests:
              cpu: "8"
              memory: 64Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "12"
              memory: 250Gi
              nvidia.com/gpu: "1"
          env:
          - name: HF_HOME
            value: "/model-cache/huggingface"
          - name: NCCL_IB_DISABLE
            value: "1"
          - name: NCCL_SOCKET_IFNAME
            value: "eth0"
          - name: NCCL_P2P_DISABLE
            value: "1"
          - name: NCCL_DEBUG
            value: "WARN"
          - name: SGLANG_ENABLE_JIT_DEEPGEMM
            value: "0"
          - name: SGLANG_DISABLE_CUDNN_CHECK
            value: "1"
          - name: SGLANG_SET_CPU_AFFINITY
            value: "true"
          - name: HF_HUB_ENABLE_HF_TRANSFER
            value: "1"
          - name: HF_XET_HIGH_PERFORMANCE
            value: "1"
          volumeMounts:
          - name: model-cache
            mountPath: /model-cache
          - name: shm
            mountPath: /dev/shm
          command: ["/bin/bash", "-c"]
          args:
          - |
            set -e
            CGPATH=$(awk -F: '$1=="0"{print $3}' /proc/1/cgroup 2>/dev/null)
            if [ -n "$CGPATH" ] && [ "$CGPATH" != "/" ]; then
              echo $((32*1024*1024*1024)) > "/sys/fs/cgroup${CGPATH}/memory.swap.max" 2>/dev/null \
                && echo "Container swap enabled (32GiB)" \
                || echo "WARNING: Could not set memory.swap.max"
            fi
            sync && echo 3 > /proc/sys/vm/drop_caches && echo 1 > /proc/sys/vm/compact_memory
            LEADER_IP=$(hostname -i | awk '{print $1}')
            echo "Leader IP: $LEADER_IP"
            echo "Pre-downloading nvidia/GLM-4.7-NVFP4 with huggingface-cli (retries until complete)..."
            until huggingface-cli download nvidia/GLM-4.7-NVFP4 \
              --cache-dir /model-cache/huggingface \
              --quiet; do
              echo "Download attempt failed, retrying in 10s..."
              sleep 10
            done
            echo "Download complete. Starting SGLang PP=2 rank 0 (leader)..."
            python3 -m sglang.launch_server \
              --model nvidia/GLM-4.7-NVFP4 \
              --pp-size 2 \
              --quantization modelopt_fp4 \
              --tool-call-parser glm47 \
              --reasoning-parser glm45 \
              --trust-remote-code \
              --chunked-prefill-size 131072 \
              --mem-fraction-static 0.80 \
              --nnodes 2 \
              --node-rank 0 \
              --dist-init-addr ${LEADER_IP}:20000 \
              --host 0.0.0.0 \
              --port 8000 \
              --max-running-requests 4 \
              --dist-timeout 7200 \
              --download-dir /model-cache/huggingface/hub
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 480   # up to ~121 min for model load
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 15
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 30
            failureThreshold: 5
            timeoutSeconds: 10
        volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: model-cache-glm-4-7-spark-01
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
    workerTemplate:
      metadata:
        labels:
          app: glm-4-7
          role: worker
      spec:
        runtimeClassName: nvidia
        nodeSelector:
          kubernetes.io/hostname: spark-02
        tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        - key: node.kubernetes.io/disk-pressure
          operator: Exists
          effect: NoSchedule
        containers:
        - name: sglang
          image: lmsysorg/sglang:nightly-dev-cu13-20260318-cb1e63ab
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
          resources:
            requests:
              cpu: "8"
              memory: 64Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "12"
              memory: 250Gi
              nvidia.com/gpu: "1"
          env:
          - name: HF_HOME
            value: "/model-cache/huggingface"
          - name: NCCL_IB_DISABLE
            value: "1"
          - name: NCCL_SOCKET_IFNAME
            value: "eth0"
          - name: NCCL_P2P_DISABLE
            value: "1"
          - name: NCCL_DEBUG
            value: "WARN"
          - name: SGLANG_ENABLE_JIT_DEEPGEMM
            value: "0"
          - name: SGLANG_DISABLE_CUDNN_CHECK
            value: "1"
          - name: SGLANG_SET_CPU_AFFINITY
            value: "true"
          - name: HF_HUB_ENABLE_HF_TRANSFER
            value: "1"
          - name: HF_XET_HIGH_PERFORMANCE
            value: "1"
          volumeMounts:
          - name: model-cache
            mountPath: /model-cache
          - name: shm
            mountPath: /dev/shm
          command: ["/bin/bash", "-c"]
          args:
          - |
            set -e
            CGPATH=$(awk -F: '$1=="0"{print $3}' /proc/1/cgroup 2>/dev/null)
            if [ -n "$CGPATH" ] && [ "$CGPATH" != "/" ]; then
              echo $((32*1024*1024*1024)) > "/sys/fs/cgroup${CGPATH}/memory.swap.max" 2>/dev/null \
                && echo "Worker container swap enabled (32GiB)" \
                || echo "WARNING: Could not set memory.swap.max"
            fi
            sync && echo 3 > /proc/sys/vm/drop_caches && echo 1 > /proc/sys/vm/compact_memory
            echo "Pre-downloading nvidia/GLM-4.7-NVFP4 with huggingface-cli (retries until complete)..."
            until huggingface-cli download nvidia/GLM-4.7-NVFP4 \
              --cache-dir /model-cache/huggingface \
              --quiet; do
              echo "Download attempt failed, retrying in 10s..."
              sleep 10
            done
            echo "Download complete. Starting SGLang PP=2 rank 1 (worker), connecting to ${LWS_LEADER_ADDRESS}:20000..."
            python3 -m sglang.launch_server \
              --model nvidia/GLM-4.7-NVFP4 \
              --pp-size 2 \
              --quantization modelopt_fp4 \
              --tool-call-parser glm47 \
              --reasoning-parser glm45 \
              --trust-remote-code \
              --chunked-prefill-size 131072 \
              --mem-fraction-static 0.80 \
              --nnodes 2 \
              --node-rank 1 \
              --dist-init-addr ${LWS_LEADER_ADDRESS}:20000 \
              --dist-timeout 7200 \
              --download-dir /model-cache/huggingface/hub
        volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: model-cache-glm-4-7-spark-02
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
