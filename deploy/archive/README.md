# Archive

Resources here are retained for reference and must not be included by active
Kustomizations or Helmfiles. Copy an archived workload back into `deploy/models/`
and update its dependencies before deploying it again.

- `models/`: retired and experimental model manifests.
- `backups/`: point-in-time operational backups.
