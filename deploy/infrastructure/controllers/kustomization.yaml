apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # CRDs must be installed first (gateway-api, inference-extension, envoy-ai-gateway)
  - gateway-api-crds.yaml
  # Gateway and networking
  - envoy-gateway.yaml
  # Auth and rate limiting
  - kuadrant.yaml
