apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: llm-buffer-policy
  namespace: token-labs
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: token-labs-gateway
  connection:
    bufferLimit: "10Mi"
